## User-Facing Docs

None. This is an internal, optional visualization path for the xarm6 planner-only blueprint. It adds no CLI, skill, MCP, or general viewer surface, and disabled behavior remains unchanged.

## Contributor Docs

None. The change does not alter contributor workflows or documented development interfaces; implementation details and the focused manual planner check belong in the change artifacts and tests.

## Coding-Agent Docs

None. No general coding-agent guidance or `AGENTS.md` behavior changes are introduced.

## Doc Validation

No documentation files require validation. If this plan is reviewed with the repository documentation checks, run `doclinks` against the changed documentation set; `md-babel-py` and `bin/gen-diagrams` are not applicable.

## No Docs Needed

No documentation updates are needed because the feature is a read-only, internal, optional Viser visualization scoped to the xarm6 planner-only manipulation path. It introduces no public command or API changes, no new inter-module stream, and no general viewer architecture.
